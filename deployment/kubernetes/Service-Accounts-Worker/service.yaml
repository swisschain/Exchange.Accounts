apiVersion: v1
kind: Service
metadata:
  labels:
    app: accounts-worker
    environment: Development
  name: accounts-worker
  namespace: exchange
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 5000
  selector:
    app: accounts-worker
  sessionAffinity: None
  type: ClusterIP
